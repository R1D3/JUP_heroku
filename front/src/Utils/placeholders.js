
export const doubleIt = `function doubleInteger(i) {
    
  // i will be an integer. Double it and return it.
  
  
  
  return i;
  
}`

export const isEven = `function isNumberEven(i) {
    
  // i will be an integer. Return true if it's even, and false if it isn't.
  
  
  
  
}`

export const getExtension = `function getFileExtension(i) {
    
  // i will be a string, but it may not have a file extension.
  // return the file extension (with no period) if it has one, otherwise false
  
  
  
}`

export const longestString = `function longestString(i) {
    
  // i will be an array.
  // return the longest string in the array
  
  
  
}`

export const arraySum = `function arraySum(i) {
    
  // i will be an array, containing integers, strings and/or arrays like itself.
  // Sum all the integers you find, anywhere in the nest of arrays.
  
  
  
}`
